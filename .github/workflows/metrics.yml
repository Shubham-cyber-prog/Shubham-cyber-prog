name: 📊 GitHub Metrics

on:
  schedule:
    - cron: "0 * * * *" # Runs every hour
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Generate GitHub Metrics
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.GH_METRICS_KEY }}
          user: Shubham-cyber-prog
          template: classic
          base: header, activity, community, repositories, metadata
          config_timezone: Asia/Kolkata
          config_display: large
          config_title: "💫 Shubham's GitHub Metrics"
          config_order: activity, community, repositories, metadata
          config_padding: 15, 25
          config_twemoji: yes

          # ---------------------
          # 🧠 GENERAL STATS
          # ---------------------
          plugin_lines: yes
          plugin_traffic: yes
          plugin_repositories: yes
          plugin_stars: yes
          plugin_followup: yes
          plugin_gists: yes
          plugin_people: yes
          plugin_contributions: yes
          plugin_reactions: yes

          # ---------------------
          # 🏆 ACHIEVEMENTS
          # ---------------------
          plugin_achievements: yes
          plugin_achievements_display: detailed
          plugin_achievements_threshold: C
          plugin_achievements_secrets: no
          plugin_achievements_limit: 10

          # ---------------------
          # 🌐 LANGUAGES
          # ---------------------
          plugin_languages: yes
          plugin_languages_colors: github
          plugin_languages_limit: 10
          plugin_languages_sections: most-used, recently-used
          plugin_languages_details: percentage, bytes-size
          plugin_languages_threshold: 0.5%
          plugin_languages_categories: programming, markup, data

          # ---------------------
          # 🕒 CODING HABITS
          # ---------------------
          plugin_habits: yes
          plugin_habits_facts: yes
          plugin_habits_charts: yes
          plugin_habits_days: 14

          # ---------------------
          # 🔥 STREAKS & TROPHIES
          # ---------------------
          plugin_streak: yes
          plugin_streak_days: 365
          plugin_trophy: yes
          plugin_trophy_rank: SS
          plugin_trophy_progress: yes

          # ---------------------
          # 🧩 CONTRIBUTION CALENDAR
          # ---------------------
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year

          # ---------------------
          # 🧑‍💻 WAKATIME (Coding Time)
          # ---------------------
          plugin_wakatime: yes
          plugin_wakatime_token: ${{ secrets.WAKATIME_API_KEY }}
          plugin_wakatime_days: 7
          plugin_wakatime_limit: 5
          plugin_wakatime_sections: time, projects, languages, editors, os

          # ---------------------
          # 🧠 LEETCODE STATS
          # ---------------------
          plugin_leetcode: yes
          plugin_leetcode_user: SubhamN2006
          plugin_leetcode_sections: solved, skills, contests, recent
          plugin_leetcode_limit_recent: 8
          plugin_leetcode_limit_skills: 10

          # ---------------------
          # 🌟 TOPICS & ORGS
          # ---------------------
          plugin_topics: yes
          plugin_topics_mode: starred
          plugin_topics_sort: stars
          plugin_topics_limit: 15
          plugin_organizations: yes
          plugin_organizations_limit: 12

          # ---------------------
          # 🧮 STARGAZERS + PINS
          # ---------------------
          plugin_stargazers: yes
          plugin_stargazers_limit: 12
          plugin_pin: yes
          plugin_pin_limit: 6

          # ---------------------
          # ⚙️ PAGE BUILD + ICONS
          # ---------------------
          plugin_pages: yes
          plugin_icons: yes
          plugin_icons_per_row: 8
          plugin_icons_size: 32

          # ---------------------
          # 📊 ACTIVITY LOG
          # ---------------------
          plugin_activity: yes
          plugin_activity_limit: 10
          plugin_activity_days: 14
          plugin_activity_filter: all
          plugin_activity_visibility: public 
