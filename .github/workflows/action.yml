name: GitHub Metrics

on:
  schedule:
    - cron: "0 */6 * * *"  # Every 6 hours for fresh updates
  workflow_dispatch:
  push:
    branches:
      - main
      - master

jobs:
  # ============================================
  # 📊 MAIN PERSONAL METRICS
  # ============================================
  github-metrics-personal:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Personal Metrics
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: Shubham-cyber-prog
          
          # Base configuration
          base: header, activity, community, repositories, metadata
          base_indepth: yes
          
          # Introduction
          plugin_introduction: yes
          plugin_introduction_title: yes
          
          # Lines of code
          plugin_lines: yes
          plugin_lines_sections: base
          plugin_lines_repositories_limit: 4
          
          # Calendar
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year
          
          # Topics
          plugin_topics: yes
          plugin_topics_mode: starred
          plugin_topics_sort: stars
          plugin_topics_limit: 20
          
          # Habits
          plugin_habits: yes
          plugin_habits_from: 200
          plugin_habits_days: 14
          plugin_habits_facts: yes
          plugin_habits_charts: yes
          plugin_habits_trim: yes
          
          # Stars
          plugin_stars: yes
          plugin_stars_limit: 4
          
          # Stargazers
          plugin_stargazers: yes
          plugin_stargazers_charts: yes
          plugin_stargazers_charts_type: chartist
          
          # Activity
          plugin_activity: yes
          plugin_activity_limit: 5
          plugin_activity_days: 14
          plugin_activity_filter: all
          plugin_activity_visibility: all
          
          # People - FIXED for better display
          plugin_people: yes
          plugin_people_types: followers, following
          plugin_people_limit: 28
          plugin_people_size: 28
          plugin_people_shuffle: yes
          plugin_people_identicons: no
          
          # Reactions
          plugin_reactions: yes
          plugin_reactions_limit: 200
          plugin_reactions_days: 14
          plugin_reactions_display: absolute
          plugin_reactions_details: percentage
          
          # Achievements
          plugin_achievements: yes
          plugin_achievements_threshold: C
          plugin_achievements_secrets: yes
          plugin_achievements_limit: 8
          plugin_achievements_display: compact
          
          # Notable
          plugin_notable: yes
          plugin_notable_from: organization
          plugin_notable_repositories: yes
          plugin_notable_indepth: yes
          
          # Fortune
          plugin_fortune: yes
          
          # Sponsors
          plugin_sponsors: yes
          plugin_sponsors_sections: goal, list, about
          
          # Traffic
          plugin_traffic: yes
          
          # Configuration
          config_timezone: Asia/Kolkata
          config_display: large
          config_animated: yes
          repositories_forks: yes
          repositories_affiliations: owner, collaborator, organization_member

  # ============================================
  # 💻 DETAILED PERSONAL METRICS
  # ============================================
  github-metrics-detailed-personal:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Personal Metrics (Detailed)
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.detailed.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: Shubham-cyber-prog
          
          # Base
          base: metadata
          
          # Gists
          plugin_gists: yes
          
          # PageSpeed (Optional - requires PAGESPEED_TOKEN)
          plugin_pagespeed: yes
          plugin_pagespeed_detailed: yes
          plugin_pagespeed_screenshot: yes
          plugin_pagespeed_pwa: yes
          plugin_pagespeed_url: .user.website
          plugin_pagespeed_token: ${{ secrets.PAGESPEED_TOKEN }}
          
          # Follow-up
          plugin_followup: yes
          plugin_followup_sections: repositories, user
          plugin_followup_indepth: yes
          plugin_followup_archived: yes
          
          # Languages - Rainbow theme
          plugin_languages: yes
          plugin_languages_indepth: yes
          plugin_languages_colors: rainbow
          plugin_languages_details: bytes-size, percentage, lines
          plugin_languages_limit: 12
          plugin_languages_sections: most-used, recently-used
          plugin_languages_threshold: 0%
          plugin_languages_recent_categories: markup, programming
          plugin_languages_recent_days: 30
          plugin_languages_recent_load: 500
          plugin_languages_analysis_timeout: 60
          
          # Dev.to Posts - YOUR USERNAME
          plugin_posts: yes
          plugin_posts_source: dev.to
          plugin_posts_limit: 4
          plugin_posts_descriptions: yes
          plugin_posts_covers: yes
          plugin_posts_user: Shubham-cyber-prog
          
          # LeetCode - YOUR USERNAME
          plugin_leetcode: yes
          plugin_leetcode_sections: solved, skills, recent
          plugin_leetcode_user: Shubham-cyber-prog
          plugin_leetcode_limit_skills: 10
          plugin_leetcode_limit_recent: 10
          
          # Discussions
          plugin_discussions: yes
          plugin_discussions_categories: yes
          plugin_discussions_categories_limit: 8
          
          # Configuration
          config_timezone: Asia/Kolkata
          config_display: large
          config_padding: 6%, 10%

  # ============================================
  # 👥 COMMUNITY & FOLLOWERS (SEPARATE FILE)
  # ============================================
  github-metrics-community:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Community & People Metrics
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.people.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: Shubham-cyber-prog
          
          base: ""
          
          # People plugin - Shows ALL followers
          plugin_people: yes
          plugin_people_types: followers, following
          plugin_people_limit: 200
          plugin_people_size: 32
          plugin_people_identicons: yes
          plugin_people_shuffle: no
          plugin_people_thanks: yes
          
          # Sponsors
          plugin_sponsors: yes
          plugin_sponsors_sections: goal, list, about
          
          # Configuration
          config_timezone: Asia/Kolkata
          config_padding: 6%, 10%

  # ============================================
  # 🔥 ACTIVITY & CODE METRICS
  # ============================================
  github-metrics-activity:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Activity & Code Metrics
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.activity.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: Shubham-cyber-prog
          
          base: ""
          
          # Activity
          plugin_activity: yes
          plugin_activity_limit: 10
          plugin_activity_days: 30
          plugin_activity_filter: all
          plugin_activity_load: 500
          plugin_activity_visibility: all
          
          # Code snippets
          plugin_code: yes
          plugin_code_lines: 20
          plugin_code_load: 500
          plugin_code_days: 7
          plugin_code_visibility: public
          
          # Configuration
          config_timezone: Asia/Kolkata

  # ============================================
  # 🌟 REPOSITORY METRICS
  # ============================================
  github-metrics-repositories:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Repository Metrics
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.repositories.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: Shubham-cyber-prog
          
          base: repositories
          
          # Stargazers
          plugin_stargazers: yes
          plugin_stargazers_charts: yes
          plugin_stargazers_charts_type: chartist
          plugin_stargazers_days: 30
          
          # Traffic
          plugin_traffic: yes
          
          # Projects
          plugin_projects: yes
          plugin_projects_limit: 10
          plugin_projects_descriptions: yes
          
          # Configuration
          config_timezone: Asia/Kolkata
          repositories_forks: yes

  # ============================================
  # 🏆 ACHIEVEMENTS METRICS
  # ============================================
  github-metrics-achievements:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Achievements Metrics
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.achievements.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: Shubham-cyber-prog
          
          base: ""
          
          # Achievements - Detailed view
          plugin_achievements: yes
          plugin_achievements_display: detailed
          plugin_achievements_secrets: yes
          plugin_achievements_threshold: B
          plugin_achievements_limit: 20
          
          # Notable contributions
          plugin_notable: yes
          plugin_notable_from: organization
          plugin_notable_repositories: yes
          plugin_notable_indepth: yes
          plugin_notable_types: commit, pull_request, issue
          
          # Configuration
          config_timezone: Asia/Kolkata

  # ============================================
  # 🎨 CREATIVE VISUALIZATIONS
  # ============================================
  github-metrics-creative:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Creative Metrics
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.creative.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: Shubham-cyber-prog
          
          base: ""
          
          # Skyline 3D
          plugin_skyline: yes
          plugin_skyline_year: 2024
          plugin_skyline_frames: 60
          plugin_skyline_quality: 0.5
          plugin_skyline_compatibility: yes
          
          # Fortune
          plugin_fortune: yes
          
          # Configuration
          config_timezone: Asia/Kolkata

  # ============================================
  # 📈 ANALYTICS (if you have tokens)
  # ============================================
  github-metrics-analytics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Analytics Metrics
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.analytics.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: Shubham-cyber-prog
          
          base: ""
          
          # PageSpeed (Optional)
          plugin_pagespeed: yes
          plugin_pagespeed_detailed: yes
          plugin_pagespeed_screenshot: yes
          plugin_pagespeed_pwa: yes
          plugin_pagespeed_url: .user.website
          plugin_pagespeed_token: ${{ secrets.PAGESPEED_TOKEN }}
          
          # WakaTime (Optional - if you use it)
          # Uncomment and add WAKATIME_TOKEN to secrets
          # plugin_wakatime: yes
          # plugin_wakatime_token: ${{ secrets.WAKATIME_TOKEN }}
          # plugin_wakatime_days: 30
          # plugin_wakatime_sections: time, projects, projects-graphs, languages, languages-graphs, editors, os
          # plugin_wakatime_limit: 10
          
          # Configuration
          config_timezone: Asia/Kolkata

  # ============================================
  # 💬 SOCIAL & CONTENT
  # ============================================
  github-metrics-social:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Social & Content Metrics
        uses: lowlighter/metrics@latest
        with:
          filename: metrics.social.svg
          token: ${{ secrets.METRICS_TOKEN }}
          user: Shubham-cyber-prog
          
          base: ""
          
          # Dev.to posts
          plugin_posts: yes
          plugin_posts_source: dev.to
          plugin_posts_limit: 6
          plugin_posts_descriptions: yes
          plugin_posts_covers: yes
          plugin_posts_user: Shubham-cyber-prog
          
          # Tweets (Optional - if you want)
          # Uncomment and add TWITTER_TOKEN to secrets
          # plugin_tweets: yes
          # plugin_tweets_token: ${{ secrets.TWITTER_TOKEN }}
          # plugin_tweets_user: your_twitter_handle
          # plugin_tweets_limit: 5
          # plugin_tweets_attachments: yes
          
          # RSS Feed (Optional - if you have a blog)
          # plugin_rss: yes
          # plugin_rss_source: https://yourblog.com/feed
          # plugin_rss_limit: 5
          
          # Configuration
          config_timezone: Asia/Kolkata
